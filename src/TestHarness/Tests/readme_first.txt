This directory should hold manually-invoked integration test scripts.

Test scripts ending with the extention ".testscript" will be run by the RUN-TESTS command on the admin console.

The build will be modified later to make these run with the build.